# 터치 디바운스 알고리즘 개선 + ESD 부품 재검토 (2026-09-14)

`ZENER_R13_THRESHOLD_TEST_20260911.md`의 후속. 오늘 진행한 (1) Littelfuse TVS 부품 재검토, (2) 마른손 5회 반복 재현성 테스트, (3) 그 결과로 인한 디바운스 알고리즘 교체를 정리.

대상: `task_MotorCtrl()` ([main.c:1728](main.c#L1728)), R13=8.2M(단독) + 제너(DZ11) 장착 보드, 임계값 2.3V(1884카운트, [main.c:1763](main.c#L1763))

---

## 1. Littelfuse SZSMFxxA(SMF 시리즈) 검토 — 부적합 판정

DZ11 대체 후보로 제시됐던 Littelfuse SZSMFxxA(SMF5.0A 등)를 조사한 결과, **채택 불가**로 결론.

| 항목 | 스펙 | 문제점 |
|---|---|---|
| 패키지 | SOD-123FL | SOD-123과 유사 풋프린트 |
| 역방향 누설전류(IR) | **400µA** | 지금까지 문제였던 DZ11(추정 수십~수백 nA)보다도 수천 배 나쁨 |
| 첨두펄스전력 | 200W @10/1000µs | **전원 레일용 서지 보호 부품** — 신호선용이 아님 |
| 클램핑 전압(실사용 서지 시) | VBR 7V보다 상당히 높음(9~12V+ 추정) | MCU 절대정격(≈5.3V) 초과 폭이 큼 — 보호 성능도 부족 |

**결론**: 이 시리즈는 자동차 배터리 라인 등 고에너지 서지를 흡수하도록 설계된 전원용 TVS라, ① 누설전류가 커서 고임피던스(8.2MΩ) 터치 노드에 치명적이고 ② 클램프비도 나빠서 신호선 보호 목적에도 부적합. **onsemi ESD5Z5.0T1G(SOD-523) 등 신호선 전용 저누설 ESD 다이오드 계열을 유지 권장** — 부품 교체가 필요해지면 이쪽으로.

---

## 2. 마른손 5회 반복 재현성 테스트

동일 조건(R13=8.2M+제너, 임계값 2.3V)에서 no touch(X-0)/마른손 touch(X-1) 쌍을 5회 반복 캡처(`g_TouchADCLog`).

### 2-1. 1차 캡처

| 회차 | 미터치 | 터치 | 분리 |
|---|---|---|---|
| 1 | 2.85V | 2.70V | 미미 |
| 2 | 2.79V | 2.78V | 거의 없음 |
| 3 | 2.76V | 2.74V | 미미 |
| 4 | 2.77V | 2.11~2.56V | 딥 있음, 2샘플만 비연속 통과 |
| 5 | 2.56V | 2.29~2.57V | 베이스라인과 거의 겹침 |

→ 5번 중 4번에서 터치해도 신호가 거의 안 움직임. 당시 디바운스(연속 6샘플, `<=19`)로는 전부 실패.

### 2-2. 재확인 캡처 (동일 조건 재시도)

| 회차 | 터치 최저값 | 임계값(1884) 이하 **연속** 샘플 수 | 당시 판정(연속 6개 필요) |
|---|---|---|---|
| 1-1 | 1.85V | 6개 | ✅ 통과(경계선) |
| 2-1 | 1.57V | 10개 | ✅ 통과 |
| 3-1 | 2.28V | 3개 | ❌ 실패 |
| 4-1 | 2.17V | 5개 | ❌ 1개 차이로 실패 |
| 5-1 | 2.19V | 4~5개 | ❌ 실패 |

→ 실제 신호는 확인됐으나(1차와 달리), **접촉이 중간에 짧게 끊기며 연속 길이가 3~10개로 들쭉날쭉** — "연속 N샘플" 방식 특유의 약점이 드러남.

---

## 3. 디바운스 알고리즘 교체 — 연속 카운트 → 윈도우 누적 카운트

### 3-1. 문제 진단

기존 방식(`rTouchChattering`, 증감식 카운터)은 **Connect 샘플이 연속으로 이어져야만** 진행이 누적됨 — 중간에 Not-Connect가 한 샘플이라도 끼면 반대 방향으로 카운터가 움직여 진행분을 깎아먹음. §2-2 데이터처럼 접촉이 짧은 조각(3~5개)으로 자주 끊기는 실사용 패턴에는 구조적으로 불리함.

### 3-2. 적용한 알고리즘

**윈도우(50샘플) 동안 Connect 여부와 무관하게 그냥 개수만 누적**하고, 윈도우 종료 시 한 번만 판정. ([main.c:1717-1781](main.c#L1717-L1781))

```c
#define TOUCH_ON_MIN_COUNT   6   // 윈도우(50샘플) 중 Connect 누적 개수 ≥6 → ON 확정
#define TOUCH_OFF_MAX_COUNT  2   // 누적 개수 ≤2 → OFF 확정
                                 // 3~5개(애매한 구간)는 이전 F_MOTOR_ON 상태 유지(히스테리시스)
```

- 제거됨: `rTouchChattering`(증감 카운터), `rTouchAlready`(윈도우 내 조기 확정 플래그)
- 신규: `rTouchConnectCnt`(윈도우 내 Connect 샘플 누적 카운터, 윈도우 시작 시 0으로 리셋)
- ON 임계 6개 / OFF 임계 2개는 §2-2의 "연속 6개 필요" 기준과 동일한 민감도를 유지하되, **연속 요구조건만 제거**한 것 — 4-1(5개), 5-1(4~5개)처럼 기존에 실패했던 케이스도 통과할 가능성이 높아짐

### 3-3. 기존 기능 유지 사항

- 임계값(2.3V/1884), `TOUCH_CHECK_WINDOW_MS`(50), `TOUCH_CHECK_PERIOD_MS`(1000)는 변경 없음
- `g_TouchADCLog` 20샘플 디버그 순환버퍼는 그대로 유지 (판정 로직과 독립적인 관찰용)
- `rPowerOnCnt`/`rInterupt_MotorStatus` 파워온 500ms 잠금 로직 변경 없음

---

## 4. 다음 확인 필요 사항

1. 재빌드 후 동일한 5회 반복(no touch/마른손) 재검증 — `TOUCH_ON_MIN_COUNT(6)` 이상 채워지는지, 미터치 시 `TOUCH_OFF_MAX_COUNT(2)` 이하로 잘 유지되는지
2. 애매한 구간(3~5개)에서 히스테리시스가 실제로 오동작 없이 잘 동작하는지 (터치 유지 중 잠깐 카운트가 떨어져도 OFF로 안 튀는지)
3. 젖은손은 지금까지 항상 여유가 컸으므로 회귀 확인 정도만
4. 이번에도 마른손 재현성이 떨어지면, §1의 SOD-523 저누설 부품 교체(onsemi ESD5Z5.0T1G)를 플랜 B로 재검토
